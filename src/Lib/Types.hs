module Lib.Types
  ( Added (..),
    ChordPattern (..),
    PitchClass (..),
    Interval (..),
    Note (..),
    NoteName (..),
  )
where

type Added = String

type ChordPattern = String

type PitchClass = Int

type Interval = Int

type Note = Int

type NoteName = String
