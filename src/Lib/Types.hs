module Lib.Types
  ( Factor (..),
    ChordPattern (..),
    PitchClass (..),
    Interval (..),
    Note (..),
    NoteName (..),
  )
where

type Factor = String

type ChordPattern = String

type PitchClass = Int

type Interval = Int

type Note = Int

type NoteName = String
